import React from 'react';

function Pop(props) {
    return (
        <div>
            <h1 style={{position:'absolute',backgroundColor:'white'}}>{props.datas}</h1>
        </div>
    );
}

export default Pop;